function func(a: number, b: number): number {
  return a + b;
}

const arrowFunc = (a: number, b: number): number => {
  return a + b;
};

function introduce(a: string = "jeonyul"): string {
  return a;
}
