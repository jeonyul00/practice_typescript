// // 타입스크립트에서의 오버로딩 : 같은 타입을 여러번 선언하는 것
// // 인터페이스, 클래스 안에서 오버로딩 가능
// declare function adddd(x: number, y: number): number;
// declare function adddd(x: number, y: number, z: number): number;
// declare function adddd(x: string, y: string, z: string): string;

// adddd(1, 2);
// adddd(1, 2, 3);
// adddd("1", "2", "3");
